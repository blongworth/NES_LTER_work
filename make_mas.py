# make_mas.py
# Script to convert from logbook format EIMS air switch files to MasterAirSwitch file format
# Logbook format is csv with timestamp, event, user, and notes in 24h GMT format
# Air Switch file is start and end of air calibration in 12h format with strings describing entry




def read_log_airfile(file):
    '''
    Read air calib file in EIMS logbook format
    '''

    return py
    
def convert_log_airfile(data):
    '''
    Convert a pandas dataframe in logbook format to MasterAirSwitch file format
    '''

    # Convert long to wide format

    # Add per-line headers

    # Convert times to 12h format

if (__name__ == _)